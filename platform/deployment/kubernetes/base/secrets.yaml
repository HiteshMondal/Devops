apiVersion: v1
kind: Secret
metadata:
  name: devops-app-secrets
  namespace: devops-app
  labels:
    app: devops-app
type: Opaque
stringData:
  # Placeholder values only. In direct/script mode, deploy_kubernetes.sh
  # overwrites these with env-supplied or randomly generated values before
  # apply. In ArgoCD/GitOps mode these are applied as-is — replace this
  # file's mechanism with a real secret manager before using it in prod.
  DB_USERNAME: "devops_user"
  DB_PASSWORD: "changeme"
  JWT_SECRET: "changeme-jwt-secret"
  API_KEY: "changeme-api-key"
  SESSION_SECRET: "changeme-session-secret"